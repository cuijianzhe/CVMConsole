/**
 * 宿主机设置说明文案（KSM / zRAM / KVM Unrestricted Guest 帮助弹窗内容）
 */

export const KSM_HELP = [
  'KSM 会在宿主机后台扫描匿名内存页，将内容完全相同的页面合并为同一份物理内存。纯虚拟化宿主机上，如果多个 VM 运行相近系统或模板，通常能节省明显内存。',
  '“均衡”是默认推荐挡位；“积极”和“极致”会更快扫描重复页，适合内存紧张或 VM 密度较高的宿主机，但会带来更多 CPU 开销。',
  '关闭 KSM 不会立即把已合并页面全部拆开，内核会在页面被写入时逐步拆分。需要彻底回收合并状态时，建议安排维护窗口后再做更激进的内核级操作。',
  '保存挡位会写入当前运行时，并通过 kvm-console-ksm.service 在宿主机重启后自动恢复。',
]

export const ZRAM_HELP = [
  'zRAM 会把一段内存作为压缩块设备并启用 swap。它不会像磁盘 swap 那样写入硬盘，适合作为虚拟化宿主机在内存压力上升时的缓冲层。',
  '面板只管理带有 kvm-zram 标签的 zRAM 设备；切换挡位时会先关闭旧的面板管理设备，再按新挡位创建。',
  '“均衡”是默认推荐挡位，逻辑容量为宿主机内存 20%，最高 32 GiB；“积极”和“极致”会给更大的压缩空间，但会占用更多 CPU。',
  '保存挡位会立即影响当前宿主机，并通过 kvm-console-zram.service 在宿主机重启后自动恢复。',
]

export const KVM_UNRESTRICTED_HELP = [
  '注意：这是宿主机级 KVM 参数，会影响当前宿主机上的所有 Intel KVM 虚拟机，并不是单台虚拟机独立配置。',
  'Unrestricted Guest 是 Intel KVM 的硬件辅助能力，通常保持启用即可，能让虚拟机更直接地运行早期启动阶段代码。',
  '在部分 VMware / ESXi 嵌套虚拟化环境中，该能力可能触发 QEMU 启动时报错，例如 KVM: entry failed, hardware error 0x7，虚拟机会进入内部错误暂停状态。',
  '遇到上述问题时，可以临时禁用该参数作为兼容性绕过。禁用后可能略微影响早期启动阶段性能，但通常比虚拟机无法启动更可接受。',
  '若当前有虚拟机正在运行或暂停，系统只会保存持久配置。需要关停所有虚拟机后重载 KVM 模块生效。',
  '若当前没有运行中的虚拟机但模块仍无法热卸载（例如系统运行在 KVM 嵌套虚拟化环境中），则需要重启宿主机后才会完全生效。',
]
