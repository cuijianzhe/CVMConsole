修改内容总结 ：

文件 修改内容 task.go 添加 GORM 标签（主键、索引、字段类型） db.go 在 AutoMigrate 中添加 &Task{} queue.go 重构任务存储逻辑

核心改动 ：

1. 双存储架构 ：任务同时存储在内存缓存和数据库中
   
   - 内存缓存：提高查询性能
   - 数据库：持久化，服务重启后恢复
2. 启动时恢复 ： Start() 函数会从数据库加载所有任务记录
3. ID 序列初始化 ：从数据库最大 ID 开始，避免重复
4. 所有操作同步 ：提交、更新、取消、清理任务都会同步到数据库
数据库表结构 （GORM 自动创建）：

字段 类型 说明 id INTEGER 主键 type VARCHAR(50) 任务类型 status VARCHAR(20) 任务状态 params TEXT 参数（JSON） result TEXT 结果（JSON） progress INT 进度 0-100 message VARCHAR(500) 状态消息 created_by VARCHAR(100) 创建人 created_at DATETIME 创建时间 updated_at DATETIME 更新时间

2026年7月22日16:48:21修改内容总结 ：

文件 修改内容 templateCategory.js 删除 FnOS/OpenWrt，添加 Other，Linux 分类添加 UOS、Kylin TemplateForm.vue 模板类型改为 Linux/Windows/Other，删除 FnOS/OpenWrt 相关配置 types.go Linux 分类添加 UOS、Kylin

修改详情 ：

1. 模板类型 ：从 4 种改为 3 种
   
   - 🐧 Linux
   - 🪟 Windows
   - 📦 Other（替代原来的 FnOS 和 OpenWrt）
2. Linux 分类 （新增 UOS、Kylin）：
   
   - Ubuntu
   - Debian
   - CentOS
   - UOS （新增）
   - Kylin （新增）
3. Other 类型特点 ：
   
   - 不显示二级分类选择
   - 不显示初始化方式配置（默认不初始化，直接复制磁盘）
原有的 FnOS 和 OpenWrt 专用初始化逻辑（ fnos_init.go 、 openwrt_init.go ）仍保留在后端，用于兼容已有的旧模板。

修改的文件：

server/service/clone/core.go - 模板类型判断逻辑，支持 Other 类型下的 FnOS/OpenWrt
server/service/template/types.go - 添加 Other 分类常量 otherTemplateCategories
server/service/template/validation.go - 更新分类验证，支持 Other 类型
server/service/template/meta.go - 更新分类标准化逻辑
server/handler/clone.go - 请求结构体添加 TemplateCategory 字段，更新参数传递
server/service/clone/types.go - CloneParams、BatchCloneParams、ReinstallParams 添加 TemplateCategory
server/service/clone/reinstall.go - 重装逻辑支持 Other 类型下的 FnOS/OpenWrt
server/service/clone/batch.go - 批量克隆传递 TemplateCategory
server/service/clone/validation.go - 添加支持分类的验证函数
逻辑说明：

选择 Other 类型 + FnOS 分类 → 走 FnOS 初始化逻辑（virt-customize 注入用户名、密码等）
选择 Other 类型 + OpenWrt 分类 → 走 OpenWrt 初始化逻辑（UCI 配置注入）
选择 Other 类型 + 其它 分类 → 直接复制模板磁盘，不做任何初始化
