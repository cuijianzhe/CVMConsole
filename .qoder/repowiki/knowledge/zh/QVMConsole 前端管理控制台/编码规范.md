- API 模块统一导出异步函数，内部调用 `request` 实例并指定 URL 与方法，部分接口支持透传临时 Token 头。
- 路由守卫在 `beforeEach` 中校验本地 Token 及角色权限（如 `adminOnly`），未授权访问强制重定向至登录页。
- Pinia Store 采用 `defineStore` 定义，状态持久化通常结合 `localStorage` 实现，并在 `logout` 动作中统一清理。
- 视图组件普遍采用 `<script setup>` 语法糖，配合 `reactive/ref` 管理表单状态，并通过 `ElMessage` 提供操作反馈。